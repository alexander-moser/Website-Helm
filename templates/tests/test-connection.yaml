apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Website-Helm.fullname" . }}-test-connection"
  labels:
    {{- include "Website-Helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Website-Helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
